name: jReleaser Release Workflow

on:
  push:
    tags:
      - 'v*'  # This triggers the workflow when a tag matching vX.X.X is pushed.

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Downgrade to Gradle 7.6
        run: |
          wget https://services.gradle.org/distributions/gradle-7.6-bin.zip
          unzip gradle-7.6-bin.zip
          echo "GRADLE_HOME=$(pwd)/gradle-7.6" >> $GITHUB_ENV
          echo "PATH=$GRADLE_HOME/bin:$PATH" >> $GITHUB_ENV

      - name: Cache Gradle packages
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle

      - name: Set up Gradle
        uses: gradle/gradle-build-action@v2

      - name: Run jReleaser Full Release
        run: ./gradlew jreleaserFullRelease
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

